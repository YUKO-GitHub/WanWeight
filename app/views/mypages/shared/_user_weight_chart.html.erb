<%= line_chart user_weight, 
    height: "250px", 
    library: { 
      title: "直近1ヶ月の体重推移", 
      scales: { 
        y: { 
          min: [0, (latest_weight.weight - 10).floor].max,
          max: (latest_weight.weight + 10).ceil,
          ticks: {
            stepSize: 0.1
          }
        },
        x: { 
          type: 'time',
          time: {
            unit: 'day',
            displayFormats: {
              day: 'MM/dd'
            },
            tooltipFormat: 'MM/dd'
          },
          ticks: {
            source: 'data'
          }
        }
      } 
    }
%>
